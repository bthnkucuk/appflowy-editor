/// AppFlowyEditor mobile helpers.
///
/// Mobile-specific icons and UI primitives (`AFMobileIcon`, etc.). The
/// mobile gesture and selection services are part of the main editor
/// surface; this entry point bundles the standalone mobile UI helpers
/// you might use outside the editor (toolbars, custom sheets).
///
/// ```dart
/// import 'package:appflowy_editor/mobile.dart';
/// ```
library;

export 'src/infra/mobile/mobile.dart';
